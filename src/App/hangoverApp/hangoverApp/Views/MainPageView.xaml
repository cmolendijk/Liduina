<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:hangoverApp.MarkupExtensions;assembly=hangoverApp"
             x:Class="hangoverApp.Views.MainPageView"
             BackgroundColor="#009688">
  <ContentPage.Content>
    <StackLayout HorizontalOptions="Center">
      <Image Source="{local:ImageResource hangoverApp.Resources.Liduina_logo.png}"/>
      <ActivityIndicator IsRunning="{Binding IsLoading}"/>
      <Label TextColor="#FFFFFF" Text="{Binding LoadingText}" HorizontalOptions="Center" />
    </StackLayout>
  </ContentPage.Content>
</ContentPage>